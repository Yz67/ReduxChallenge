/*
    /app/redux/actions/types.js
    Exports the specific strings defining the types that our reducer will accept to avoid
    typos when generating actions and catching actions
*/
const types = {
    SET_START: 'SET_START',
    SET_APPOINTMENT: 'SET_APPOINTMENT',
    SET_DAY: 'SET_DAY',
};

export default types;
