import * as types from '../actions/types';
import initialState from './initialState';

const scheduling = (state = initialState, action) => {
    switch (action.types) {
        case types.default.ADD_CALL:

    }
}

export scheduling
